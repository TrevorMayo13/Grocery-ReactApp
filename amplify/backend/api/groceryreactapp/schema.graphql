type Item @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  description: String
  image: String
  price: Float
  category: String
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
}
 